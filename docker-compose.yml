version: '3'

services:
  backend:
    image: crisncris0000/portfolio_backend
    platform: linux/amd64
    ports:
      - 8080:8080
    container_name: backend_c
  
  frontend:
    image: crisncris0000/portfolio_frontend
    platform: linux/amd64
    container_name: frontend_c
    ports:
      - 3000:3000